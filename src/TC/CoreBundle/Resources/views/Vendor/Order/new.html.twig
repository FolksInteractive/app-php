{% extends 'TCCoreBundle:Vendor:Relation/index.html.twig' %}

{% use 'TCCoreBundle:Vendor:Order/form.html.twig' with bottom_js as form_bottom_js %}

{% block toolbar %}
    <div class="tc-breadcrumb pull-left">        
        {% set menu = knp_menu_get('TCCoreBundle:VendorMenuBuilder:ordersMenu',[],{"relation": relation}) %}
        {{ knp_menu_render(menu, {'template': 'TCCoreBundle::breadcrumbs.html.twig'}) }}
    </div>

    <div class="tc-controls pull-right">
        {{ block('controls') }}
    </div>
{% endblock %}

{% block bottom_js %}
{{ parent() }}
{{ block('form_bottom_js') }}
{% endblock %}